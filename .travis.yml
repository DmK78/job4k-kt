dist: trusty
language: java
jdk:
  - openjdk11
env:
  - SCRIPT=bin/unit-tests.sh

  - SCRIPT=bin/run-journey-test-from-ci.sh

# http://docs.travis-ci.com/user/migrating-from-legacy
sudo: false

addons:
  apt:
    packages:
      - tree

before_install:
  - rvm install 2.2.5
  - rvm use 2.2.5
  - bin/build-jq.sh  # we want jq 1.5 features; not avail through pkg repo.

# https://docs.travis-ci.com/user/customizing-the-build#Skipping-the-Installation-Step
install: true  # if we don't skip install, ./gradlew assemble is invoked, but this task is not available.

before_script:
  - export PATH=$TRAVIS_BUILD_DIR/bin:$PATH  # ensure our tools are prefered over included ones.

script:
  - $SCRIPT

# configure caching (https://docs.travis-ci.com/user/languages/java/#Caching)
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
# format from https://docs.travis-ci.com/user/caching/#Enabling-multiple-caching-features
cache:
  bundler: true
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/


# используем язык java. Travis CI поддерживает другие языки тоже.
#language: java
#jdk:
  #- openjdk11
#  - oraclejdk8 before_install: - chmod +x gradlew - chmod +x gradle/wrapper/gradle-wrapper.jar script: - ./gradlew test build

# используем режим обычного пользователя для сборки проекта.
#sudo: false

# после выполнения сборки запустить скрипт, который будет собирать статистику по JaCoCo
# Здесь используется сторонни сервис https://codecov.io/
#after_success:
#  - bash <(curl -s https://codecov.io/bash)

# Отправлять уведомление на почтуyouremail@smtp.ru(Изменить на свою) если сборка упала
#notifications:
#  email:
#    recipients:
#      -dmk78@inbox.ru
#    on_success: never # default: change
#    on_failure: always # default: always
#env:
#  global:
#    - CODECOV_TOKEN="1f9a1cfa-9785-48cf-b78c-2fa2742f982e"



